name: Merge PR

on:
  pull_request:
    types: [closed]

jobs:
  say_pr_merged:
    runs-on: ubuntu-latest
    if: ${{ github.event.pull_request.merged == true }}
    steps:
      - name: Message
        run: echo "Pull Request mergée !"
